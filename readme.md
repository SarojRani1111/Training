Task1:
Installeed git in local machin then attached screenshots of all installation steps:
Then checked git version by git --version

Task2:
Configured config global and local config and attached screenshot image

Task3:
Created a directory with name git-training and initialized wit git init command
Then created two files inside this directory with name reademe.md and app.py
First checked status of current directory by using git status
It showed two files untracked 
Then staged by using git add .
Then commited with message
Checked log gy using git log and attached log Screenshot

Task:
Created one new branch with name contact by usin git branch contact
Then switched to contact branch inside contact branch created file and did some changes
then again switched to main branch
Then created PR and attached screen shot


